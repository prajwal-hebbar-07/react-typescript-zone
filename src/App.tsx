import CourseGoal from "./components/CourseGoal";

export default function App() {
  return (
    <main>
      <CourseGoal
        title="React + TS"
        description="Try react with typescript to make the applications more robust!"
      />
    </main>
  );
}
